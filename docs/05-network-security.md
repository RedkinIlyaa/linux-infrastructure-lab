
# Goal

Learn basic Linux security concepts including SSH authentication, sudo privileges and firewall configuration.

## SSH Authentication

Verified SSH key authentication using GitHub.

## Sudo Privileges

Inspected sudo group membership and verified administrative access control.

## Firewall Configuration

Configured UFW to allow SSH access before enabling the firewall.

## Screenshots

- ssh-key-authentication.png
- sudo-access-control.png
- sudoers.png
- sudo-group-members.png
- ufw-allow-OpenSSH.png
